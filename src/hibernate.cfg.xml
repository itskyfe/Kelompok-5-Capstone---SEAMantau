<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- === Database Connection === -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/db_seamantau?useSSL=false&amp;serverTimezone=Asia/Jakarta
        </property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property>

        <!-- === JDBC Connection Pool === -->
        <property name="hibernate.connection.pool_size">5</property>

        <!-- === SQL Dialect === -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- === Hibernate Options === -->
<property name="hibernate.connection.pool_size">10</property>

        <property name="hibernate.hbm2ddl.auto">none</property>

        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.use_sql_comments">true</property>


        <!-- === Entity Mappings === -->
        <mapping class="model.User"/>
        <mapping class="model.Pegawai"/>
        <mapping class="model.Nelayan"/>
        <mapping class="model.Admin"/>
        <mapping class="model.Kapal"/>
        <mapping class="model.Laporan"/>
        <mapping class="model.LaporanPengaduan"/>
        <mapping class="model.WilayahTangkap"/>

    </session-factory>
</hibernate-configuration>
